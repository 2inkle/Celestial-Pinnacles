# diff-cave-boss-balance-risk-p0-2026-08-25.md — [P0] 동굴 보스 밸런스 리스크 (2026-08-25)

⚠ 새 명명 규칙 실천(브랜치 고유 파일명). 병합 검토 끝나면
`git rm diff-cave-boss-balance-risk-p0-2026-08-25.md`로 제거 권장.

- **기준 브랜치**: `main`(커밋 `678b90c`, 드리프트 없음)
- **작업 브랜치**: `cave-boss-balance-risk-p0-2026-08-25`(1개 커밋)
- **변경 파일**: `CLAUDE.md` 하나뿐(코드 변경 없음, 순수 설계 문서)

## 배경 요약 — ⚠ 우선순위 P0

동굴 보스(러프 스탯 HP52,000/DEF·MDEF 35%)의 실질 위력을 검증하려고
실제 캐릭터 패턴 데이터를 대입한 "로직 테스트"를 두 건 진행함.

**⚠ 중요 caveat**: 계산은 이 저장소의 `skill-table.json` 스냅샷
기준이다. 이 프로젝트는 2026-08-16부터 `skill-table-editor.html`이
`game_content` DB(라이브, SQL 마이그레이션으로만 갱신)를 보여주는
조회 전용 뷰어로 전환됐으므로, `skill-table.json`이 라이브 값과
괴리돼 있을 가능성이 있다 — **주 워크스페이스에서 `game_content`의
실제 Lightning Ball/Hurricane Shot 수치를 반드시 재확인해야 함.**

**케이스 A(둠로드, 30레벨, 고투자+2차전직+자원관리 로테이션)**: 7턴
만에 보스 처치 — 문제없음, 의도된 강함.

**케이스 B(스나이퍼, 20레벨, +8 합성궁+바닐라 화살통, 무버프)**: 단
한 번의 행동(Hurricane Shot)만으로 보스 HP의 **24.2%**를 깎음(기존
"연타점감 1회만 적용" 방식 기준). SP 무제한 가정 없이는 자원 부족으로
1발 쏘고 영구 무력화되는 것까지 확인(자원관리 없는 패턴의 당연한
귀결). **이게 진짜 P0** — "버프 없는 저투자 캐릭터의 기본 성능"만으로
동굴 보스가 위협받는다는 뜻.

**제작 기댓값**: 합성궁+8 = 35,000G(기댓값), 스태프+11 = 약
1,059,800G(약 30배) — `web/refinery.html`의 성공률/파괴/재구매 공식을
정밀 적용해 계산.

**연타점감 복리화 실험**: "1회만 감쇠 후 유지" → "매 히트 복리(×0.6^n)"
로 바꾸면 Hurricane Shot 1회 총합이 24.2%→6.0%로 줄어듦(다만 9~16번째
히트 합이 46 데미지뿐이라 "16히트 스킬"의 정체성이 무의미해지는 과교정
위험도 있음 — 동굴 전용 사안이 아니라 기존 스킬표 전체의 코어
메커니즘이라 신중한 검토 필요).

**경제적 재해석(사용자 통찰)**: 공략 성공만이 목표라면, 30배 비싼
무기 하나(둠로드)에 투자하기보다 같은 돈으로 저렴한 딜러를 여러 명
(스나이퍼) 육성하는 쪽이 합리적 — 그 관점에서 스나이퍼 1인의 행동당
6%(연타점감 복리화 가정)는 합리적으로 보이지만, **현재 라이브 상태
그대로(24.2%)라면 문제**라는 게 최종 결론.

전체 배경/근거·정밀 계산 과정은 `CLAUDE.md`의 새 섹션 "[P0 밸런스
리스크] 동굴 보스 DEF/HP가 '무버프 평범한 캐릭터' 기준으로도 너무
낮음"에 그대로 있음(아래 diff 참고).

## 병합 전 체크리스트

`CLAUDE.md` 순수 텍스트 추가라 코드 실행 검증 대상 아님. `node --check`/
`demo-*.js` 불필요 — 이번 커밋엔 실행 코드 변경이 없음.

**병합 시 최우선 확인**: 병합 직후 `game_content` DB에서 Lightning
Ball/Hurricane Shot의 실제 라이브 수치(coefficient/hits/diminishPerHit
등)를 조회해서, 이 문서의 계산 전제가 여전히 유효한지부터 재확인할 것
— 라이브 값이 다르면 재계산 필요.

## 병합 방법

```bash
git fetch origin
git show origin/cave-boss-balance-risk-p0-2026-08-25:CLAUDE.md | head -105   # 새 섹션 확인
git merge origin/cave-boss-balance-risk-p0-2026-08-25
git rm diff-cave-boss-balance-risk-p0-2026-08-25.md
git commit
```

## 전체 diff

```diff
diff --git a/CLAUDE.md b/CLAUDE.md
index 915e80a..9636bc8 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -6,6 +6,97 @@ JS로 만드는 턴제 전투 시뮬레이션 웹게임. 패턴 빌드로 스킬
 테마(마을→왕국→그 뒤) 하나만 구현돼 있고, 이걸로 엔진과 성장곡선이
 유효한지 검증하는 게 목표.
 
+## [P0 밸런스 리스크] 동굴 보스 DEF/HP가 "무버프 평범한 캐릭터" 기준으로도 너무 낮음 (2026-08-25)
+
+동굴 보스(러프 스탯 HP52,000/DEF·MDEF 35%, `cave-monster-chain-concept-
+2026-08-24`)의 실질 위력을 검증하려고, 사용자 요청으로 실제 패턴
+데이터를 대입한 "로직 테스트" 두 건을 진행함. **⚠ 아래 계산은 이
+저장소의 `skill-table.json` 스냅샷 기준이다 — 이 프로젝트는 2026-08-16
+부터 `skill-table-editor.html`이 `game_content` DB(라이브, SQL
+마이그레이션으로만 갱신)를 보여주는 조회 전용 뷰어로 전환됐으므로,
+`skill-table.json`이 라이브 값과 괴리돼 있을 가능성이 있음 — 주
+워크스페이스에서 `game_content`의 실제 Lightning Ball/Hurricane Shot
+수치 재확인 필수.**
+
+### 케이스 A — 30레벨 둠로드(마법공격력86, INT 75+10+6, SPD100): 7턴 처치, 문제없음
+
+패턴: SP<300→High Mana Recharge / 최초1회→SpellFocus / (집속마력≥20
+AND SP≥200, 최초1회)→Mana Flood / 항상→Lightning Ball. **로직 함정**:
+3번째 행이 `andNext:true`라 `"action":"Lightning Ball"`은 죽은
+필드고, 실제로는 조건만 다음 행(Mana Flood)에 AND로 이어붙는다.
+
+엔진 실제 상수(`GAUGE_THRESHOLD=100,000`, 틱당 게이지 충전=
+`effectiveSpeed`, preDelay는 게이지가 얼어붙는 순수 대기, postDelay는
+게이지에서 `postDelay×effectiveSpeed`만큼 추가 차감 — 결과적으로
+"다음 행동까지 걸리는 시간 = preDelay+postDelay+100,000/effectiveSpeed")
+로 정밀 틱 계산: t=1000 SpellFocus, t=2025 Lightning Ball①(14,760
+데미지), t=3030 Mana Flood(bonusInt 6→256, effInt 91→341), t=4070
+High Mana Recharge(SP회복), t=5095 Lightning Ball②(32,613 데미지,
+HP 52,000→4,627), t=6125 Lightning Ball③ 첫 히트에서 사망. 엔진의
+실제 턴(1,000틱 단위 전역 버킷) 기준 **7턴째** 사망.
+
+**결론(사용자 재확인)**: 이건 문제없다 — 레벨 상한+2차 전직+자원관리
+로테이션 실행까지 다 갖춰야 나오는 "의도된 강함"이라 정당한 페이오프.
+
+### 케이스 B — 20레벨 스나이퍼(+8 합성궁+바닐라 화살통, DEX57+1, 무버프): 진짜 문제
+
+패턴: Hurricane Shot(물리, DEX 계수0.9, targetCount `"all"`, hits16,
+diminishPerHit40%, invalid, SP90+화살16, preDelay30/postDelay50) 스팸.
+
+- **SP 제한이 있으면**: `maxSp=baseSp50+floor(realInt×10)`인데 INT
+  미투자(기본10)라 maxSp=150 — Hurricane Shot 1회(SP90) 쏘면 SP60
+  남아 두 번째부턴 영구히 "코스트 부족으로 발동 실패"(`resolvePreparedSkill`
+  442-444행 — 실패해도 postDelay는 그대로 소모돼서 매 사이클 헛스윙만
+  반복). **패턴에 자원관리 스킬이 없으면 아무리 센 스킬도 무용**이라는
+  이 게임의 "패턴 퍼즐" 설계 원칙이 정확히 의도대로 작동한 사례.
+- **SP 무제한 가정 시**: effectiveAtk=75(합성궁+8의 ATK 48×1.256≈60
+  + 화살통 15), effectiveDex=58, dampDamageStat(58)≈28.71 →
+  Hurricane Shot 1회(16히트, 기존 "1회만 감쇠 후 유지" 방식) =
+  **12,584 데미지 = 보스 HP의 24.2%**, 단 한 번의 행동으로.
+
+**제작 기댓값**(`web/refinery.html` 확정 공식 — 레벨7부터 성공률
+50%→매단계×0.8, 하한20%, 실패 시 완전 파괴+처음부터 재시작, 비용=
+상점가/10×실제시행횟수 — 재구매 비용까지 포함해 정밀 계산):
+
+| 목표 | 상점가 | 최종단계 성공확률 | 기대 총 골드 |
+|---|---|---|---|
+| 합성궁 **+8** | 4,000G | 20% | **35,000G** |
+| 스태프 **+11**(둠로드 케이스) | 2,000G | 0.34% | **≈1,059,800G**(약 30배) |
+
+**연타점감을 "1회만 깎고 유지"에서 "매 히트 복리(×0.6^n)"로 바꾸면**:
+Hurricane Shot 1회 총합이 12,584(24.2%)→**3,137(6.0%)**로 줄어듦.
+다만 9~16번째 히트 합이 46 데미지밖에 안 돼서, "16히트 스킬"이 사실상
+"6~7히트 스킬"과 다를 바 없어지는 과교정 위험도 있음 — 이 자체는
+동굴 전용이 아니라 기존 스킬표(Hurricane Shot 등)의 코어 메커니즘이라
+동굴 하나만 보고 고칠 사안은 아님.
+
+**경제적 재해석(사용자 핵심 통찰)**: "가성비가 무너졌다"는 지적은
++11 스태프 쪽 얘기였음 — 상점가의 약 30배(≈106만 골드)라는 기대비용은
+그 자체로 "정상 가성비 붕괴" 구간이고, 공략 성공만이 목표라면 굳이
+그 비용을 들여 둠로드 하나에 몰빵하기보다 **같은 돈으로 스나이퍼를
+여러 명(스태프 1개 가격이 스나이퍼 세트 2인분의 약 15배) 육성하는
+쪽이 경제적으로 합리적** — 그 관점에서 보면 스나이퍼 1인의 행동당
+6%(연타점감 복리화 가정)는 오히려 합리적인 수치로 보임. **반대로,
+현재 라이브 상태 그대로(1회만 감쇠, 24.2%)라면 "버프 하나 없는 저투자
+캐릭터의 기본 성능"만으로 동굴 보스가 위협받는다는 뜻이라 이쪽이 진짜
+P0.**
+
+### 결론
+- 둠로드 케이스: 문제없음, 기록만.
+- **스나이퍼 케이스: P0** — 동굴 보스 DEF/MDEF·HP가 "무버프 평범한
+  중간레벨 캐릭터의 기본 성능" 기준으로도 너무 낮다. 보스 개별 수치
+  조정만으로 끝날 사안이 아니라, **"targetCount:다중+다단히트" 조합
+  스킬이 적이 1마리뿐인 솔로 보스전에서 어떻게 작동해야 하는가**(연타
+  점감 복리화 여부 등)까지 함께 검토해야 함. 사용자 총평: "전체적으로
+  밸런싱이 잡힐 필요가 있어보인다."
+
+### 다음 세션에서 이어갈 것
+주 워크스페이스에서: (1) `game_content` DB의 Hurricane Shot/Lightning
+Ball 실제 라이브 수치 재확인(이 저장소 스냅샷과 다를 수 있음), (2)
+`simulate.js`로 이 두 케이스 재현, (3) 연타점감 복리화 여부를 게임
+전체 스킬표 관점에서 결정, (4) 동굴 보스(및 향후 몬스터 전반)의 HP/
+DEF를 "평범한 무버프 캐릭터 기본 성능" 기준으로 재산정.
+
 ## Sheet 스킬 카드 효과 표시 — "27종 전수 등록"(2026-08-22) 이후에도 남아있던 누락 2건 수정 (2026-08-24)
 
 바로 아래(2026-08-22) "실전투 신고 4건 일괄 수정"의 4번 항목("Sheet 화면
```

## 다음 세션에서 이어갈 것 (구현 미착수)

주 워크스페이스에서: (1) `game_content` DB의 Hurricane Shot/Lightning
Ball 실제 라이브 수치 재확인, (2) `simulate.js`로 두 케이스 재현·검증,
(3) 연타점감 복리화 여부를 게임 전체 스킬표 관점에서 결정, (4) 동굴
보스(및 향후 몬스터 전반) HP/DEF를 "평범한 무버프 캐릭터 기본 성능"
기준으로 재산정.
