# diff-cave-lowfloor-table-review-2026-08-25.md — 동굴 저층 러프 스탯표 재검토 통과 (2026-08-25)

⚠ 새 명명 규칙 실천(브랜치 고유 파일명). 병합 검토 끝나면
`git rm diff-cave-lowfloor-table-review-2026-08-25.md`로 제거 권장.

- **기준 브랜치**: `main`(커밋 `678b90c`, 드리프트 없음)
- **작업 브랜치**: `cave-lowfloor-table-review-2026-08-25`(1개 커밋)
- **변경 파일**: `CLAUDE.md` 하나뿐(코드 변경 없음, 순수 설계 문서)

## 배경 요약

동굴 하위층(1~4층) 설계를 이어가던 중, 사용자가 스코프를 정리함 — 이
구간은 정밀한 밸런싱이 필요 없고("죽지 않고 데미지를 넣을 수 있다면
문제없다"), 지난 세션의 러프 스탯표(A~I, `cave-monster-chain-
concept-2026-08-24`에서 확정, 이미 main에 병합됨)를 직전에 확정한
"Invalid 스킬 밸런스 원칙"(D/F/I — 명중/빈도/위력 중 최소 하나는
반드시 깎을 것, `cave-invalid-skill-design-2026-08-25`)에 대입해서
재검증한 뒤 통과시키는 리뷰.

**결론: 표 전체를 그대로 통과, 숫자 변경 없음.** D/F/I 셋 다 이미
원칙에 부합함을 raw 데미지·명중률 기반 기댓값 계산으로 확인(자세한
계산은 아래 diff 참고). 유일하게 남긴 건 실제 데이터 작성 시 지킬
체크리스트 두 가지 — F에 `invalid:true` 필수(이번 세션에 새로 발견:
가드 탱커가 있으면 Invalid 없이는 다단히트가 전부 탱커한테만 몰림),
I에 눈에 띄게 긴 `postDelay`. 정확한 수치와 이 두 필드의 정밀 조정은
사용자가 **주 워크스페이스에서 `simulate.js`로 직접 실측하며 진행**
하기로 확정 — 이 샌드박스에서는 여기까지만.

## 병합 전 체크리스트

`CLAUDE.md` 순수 텍스트 추가라 코드 실행 검증 대상 아님. `node --check`/
`demo-*.js` 불필요.

## 병합 방법

```bash
git fetch origin
git show origin/cave-lowfloor-table-review-2026-08-25:CLAUDE.md | head -50   # 새 섹션 확인
git merge origin/cave-lowfloor-table-review-2026-08-25
git rm diff-cave-lowfloor-table-review-2026-08-25.md
git commit
```

## 전체 diff

```diff
diff --git a/CLAUDE.md b/CLAUDE.md
index 915e80a..0f0d34b 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -6,6 +6,38 @@ JS로 만드는 턴제 전투 시뮬레이션 웹게임. 패턴 빌드로 스킬
 테마(마을→왕국→그 뒤) 하나만 구현돼 있고, 이걸로 엔진과 성장곡선이
 유효한지 검증하는 게 목표.
 
+## 동굴 저층 러프 스탯표 재검토 — D/F/I 원칙과 부합 확인, 통과 (2026-08-25)
+
+동굴 하위층(1~4층) 설계를 이어가던 중, 사용자가 스코프를 정리함: "이
+구간은 대충 정해도 된다. 죽지 않고 데미지를 넣을 수 있다면 문제
+없다." — 그래서 지난 세션(`cave-monster-chain-concept-2026-08-24`,
+이미 병합됨)의 러프 스탯표를 다시 훑어보고 "실질적 위력"이 말이
+되는지만 확인한 뒤 통과시키는 리뷰를 진행함. 직전에 확정한 "Invalid
+스킬 밸런스 원칙"(D/F/I — 명중/빈도/위력 중 최소 하나는 반드시 깎을
+것)을 이 표에 실제로 대입해서 부합하는지가 핵심.
+
+**재검토 결론 — 표 전체를 그대로 통과, 숫자 변경 없음**:
+- **D**(raw≈214): A(≈307)/E(≈453)보다도 낮음 — "위력" 축이 이미
+  충분히 깎여 있어 Invalid인데도 "경고사격" 포지션과 정확히 맞음.
+- **F**(히트당≈145, 명중 20%, 6히트): 기댓값 `6×0.2×145≈174`,
+  최악의 경우(6히트 전부 명중, 확률 0.2^6≈0.006%)조차 `870`으로 I
+  한 슬롯 수준에 그침 — "확실하게" 축이 명중률로 이미 강하게 깎여
+  있어 원칙에 부합.
+- **I**(raw≈839): 세 슬롯 중 가장 높지만 4층 "복합 위협" 포지션으로
+  의도된 것이고, "자주" 축을 postDelay로 깎기로 이미 합의함(raw가
+  세도 자주 못 쏘면 총 위협량은 완만해짐 — A/E/G 등에 이미 적용됐던
+  "raw는 방어구 0 기준, 1~2개 장비면 안 죽는다" 논리 그대로).
+- **A/B/C/E/G/H**: 전부 지난 세션에 이미 "문제없음"으로 승인된
+  4항목 피드백 그대로 — 다시 건드릴 이유 없음.
+
+**남기는 체크리스트**(다음 단계, 실제 `skill-table.json`/`monster-
+roster.html` 데이터 작성 시): F에 `invalid:true` 반드시 포함(이번
+세션에 새로 발견 — Invalid 없이는 가드 탱커가 있으면 다단히트가 전부
+탱커한테만 몰림), I에 postDelay를 눈에 띄게 길게(다른 일반 공격의
+2~3배 수준). 정확한 수치(스탯 전반 + 이 두 필드의 정밀 조정)는 이
+샌드박스가 아니라 **주 워크스페이스에서 `simulate.js`로 실측하며
+진행**하기로 확정.
+
 ## Sheet 스킬 카드 효과 표시 — "27종 전수 등록"(2026-08-22) 이후에도 남아있던 누락 2건 수정 (2026-08-24)
 
 바로 아래(2026-08-22) "실전투 신고 4건 일괄 수정"의 4번 항목("Sheet 화면
```

## 다음 세션에서 이어갈 것 (구현 미착수)

주 워크스페이스에서 `simulate.js`로 A~I 러프 수치를 실측 검증하며
정밀 조정, 그 결과를 바탕으로 실제 `web/monster-roster.html`/
`skill-table.json` 데이터 작성(F의 `invalid:true`, I의 긴 `postDelay`
포함). 몬스터 한글 이름도 이때 함께 확정.
