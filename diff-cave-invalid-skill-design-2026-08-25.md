# diff-cave-invalid-skill-design-2026-08-25.md — 동굴 D/F/I Invalid 밸런스 원칙 (2026-08-25)

⚠ 새 명명 규칙 실천(브랜치 고유 파일명). 병합 검토 끝나면
`git rm diff-cave-invalid-skill-design-2026-08-25.md`로 제거 권장.

- **기준 브랜치**: `main`(커밋 `678b90c`, 드리프트 없음)
- **작업 브랜치**: `cave-invalid-skill-design-2026-08-25`(1개 커밋)
- **변경 파일**: `CLAUDE.md` 하나뿐(코드 변경 없음, 순수 설계 문서)

## 배경 요약

동굴 하위층(1~4층) 설계로 내려가면서, 사용자가 먼저 스코프를 정리함
— 이 티어는 "정밀한 하한선 보장"이 아니라 "구조가 제대로 서있는가"만
맞추면 되고, 실제 문제가 생기면 파밍/장비로 급한 불을 끌 수 있으니
정밀한 수치 밸런싱은 필요 없다는 기존 원칙(cave-monster-chain-concept
브랜치, 이미 병합됨)의 재확인.

다만 D/F/I 세 슬롯 전부 `skill.invalid`(전열 보호 무시, "차단 불가")를
쓰는데, 이건 파티의 핵심 방어 수단인 가드 탱커를 그냥 우회하는 효과라
"약하면 위협이 안 되고, 강하면 파티 생존률이 말도 안 되게 떨어진다"는
별도 질적 위험이 있다는 지적으로 아래 원칙을 확정함:

**Invalid 스킬은 "확실하게+자주+세게"를 동시에 가지면 안 됨** — 명중/
빈도/위력 세 축 중 최소 하나는 반드시 깎여야 함.

- D(2→3층 이월) = 위력 낮음("세게" 축을 깎음)
- F(3→4층 이월) = 초저명중("확실하게" 축을 깎음) — **이번에 F도
  Invalid가 필요함을 새로 발견**: `targetCount:"single"`+`hits:N`은
  가드 탱커가 있으면 매 히트가 전부 탱커한테만 몰려서 "무작위로
  떨어지는 낙석"이라는 서사가 깨짐. 다행히 이미 확정해둔 초저명중이
  그대로 원칙의 "확실하게" 대가 역할을 겸함 — 새 페널티 불필요.
- I(4층) = 긴 postDelay("자주" 축을 깎음, 5층 보스의 "느리지만 무거운"
  패턴과 같은 원리 재사용)

정확한 수치(위력/명중률/postDelay 틱수)는 여전히 미정 — 이번엔 "원칙
자체가 문제 없어 보인다"는 확인만 받은 단계. 전체 배경은 `CLAUDE.md`의
새 섹션 "동굴 저층(1~4층) Invalid 스킬 밸런스 원칙 확정 — D/F/I"에
그대로 있음(아래 diff 참고).

## 병합 전 체크리스트

`CLAUDE.md` 순수 텍스트 추가라 코드 실행 검증 대상 아님. `node --check`/
`demo-*.js` 불필요 — 이번 커밋엔 실행 코드 변경이 없음.

## 병합 방법

```bash
git fetch origin
git show origin/cave-invalid-skill-design-2026-08-25:CLAUDE.md | head -60   # 새 섹션 확인
git merge origin/cave-invalid-skill-design-2026-08-25
git rm diff-cave-invalid-skill-design-2026-08-25.md
git commit
```

## 전체 diff

```diff
diff --git a/CLAUDE.md b/CLAUDE.md
index 915e80a..a6a8185 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -6,6 +6,44 @@ JS로 만드는 턴제 전투 시뮬레이션 웹게임. 패턴 빌드로 스킬
 테마(마을→왕국→그 뒤) 하나만 구현돼 있고, 이걸로 엔진과 성장곡선이
 유효한지 검증하는 게 목표.
 
+## 동굴 저층(1~4층) Invalid 스킬 밸런스 원칙 확정 — D/F/I (2026-08-25)
+
+동굴 하위층(1~4층) 설계로 내려가면서, 사용자가 먼저 스코프를 정리함:
+"밸런스는 문제없는 것으로 친다 — 막히면 파밍하고 아이템 하나 장착하는
+것으로 급한 불은 꺼지니까, 실제로 문제가 생기면 그때 밸런싱해도 된다."
+1티어는 "정밀한 하한선 보장"이 아니라 "구조가 제대로 서있는가"만
+맞추면 되는 단계라는 기존 원칙(cave-monster-chain-concept 참고)과
+정확히 같은 결의 재확인. 다만 D/F/I 세 슬롯이 전부 `skill.invalid`
+(전열 보호 무시, "차단 불가" — `src/skillResolution.js:75,195-200`)를
+쓰는데, 이건 파티의 핵심 방어 수단(가드 탱커)을 그냥 우회해버리는
+효과라 "약하면 위협이 안 되고, 강하면 파티 생존률이 말도 안 되게
+떨어진다"는 별도의 질적 위험이 있다는 지적으로 원칙을 하나 확정함.
+
+**원칙**: Invalid 스킬은 "확실하게+자주+세게"를 동시에 가지면 안 됨 —
+가드를 우회하는 대신 세 축(명중/빈도/위력) 중 최소 하나는 반드시
+깎여있어야 함.
+
+- **D**(2→3층 이월, "위협사격" 수준): 단일 Invalid + **위력을 낮게**
+  (하위 층다운 경고사격 정도). "세게" 축을 깎음.
+- **F**(3→4층 이월, 다단히트+초저명중 낙석): 기존엔 그냥 "무작위
+  재대상"으로만 설계했었는데, 이번에 재확인한 결과 **F도 Invalid가
+  필요함을 발견** — `targetCount:"single"`+`hits:N`은 가드 탱커가
+  있으면 `resolveTargets()`(`skillResolution.js:202-207`)가 매 히트를
+  전부 그 탱커에게만 몰아줘서 "무작위로 떨어지는 낙석"이라는 서사
+  자체가 깨짐. Invalid로 가되, 이미 확정해둔 **초저명중**이 그대로
+  "확실하게" 축을 깎는 대가 역할을 겸함(새 페널티 불필요, 기존 설계가
+  자동으로 원칙을 만족).
+- **I**(4층, Invalid단일+가끔자강화 복합 위협): 단일 Invalid + 4층답게
+  위력은 D보다 세도 되지만, **postDelay를 길게** 줘서 자주 못 쏘게
+  (5층 보스의 "느리지만 무거운" 강타와 같은 원리 재사용). "자주" 축을
+  깎음.
+
+D=약한 한방/F=거의 안 맞는 다단히트/I=가끔 오는 센 한방 — 세 슬롯이
+서로 다른 방식으로 대가를 치르게 해서, 개별로도 안전하고 층을 오가며
+서로 다른 위협 체감을 준다는 게 자연스럽게 유지됨. 정확한 수치(위력/
+명중률/postDelay 틱수)는 여전히 미정 — 위 "원칙 자체가 문제 없어
+보인다"는 확인만 받은 단계.
+
 ## Sheet 스킬 카드 효과 표시 — "27종 전수 등록"(2026-08-22) 이후에도 남아있던 누락 2건 수정 (2026-08-24)
 
 바로 아래(2026-08-22) "실전투 신고 4건 일괄 수정"의 4번 항목("Sheet 화면
```

## 다음 세션에서 이어갈 것 (구현 미착수)

D/F/I를 포함한 A~I 전 슬롯의 실제 스킬 정의(위력/명중률/preDelay·
postDelay 틱수 등 구체적인 수치)를 확정해서 `skill-table.json`/
`web/monster-roster.html`에 작성. 이번 브랜치는 원칙만 확정하고
수치는 아직 안 채움.
