# diff-defense-formula-consideration-2026-08-25.md — 방어력 공식 개편 고려사항 격상 (2026-08-25)

⚠ 새 명명 규칙 실천(브랜치 고유 파일명). 병합 검토 끝나면
`git rm diff-defense-formula-consideration-2026-08-25.md`로 제거 권장.

- **기준 브랜치**: `main`(커밋 `678b90c`, 드리프트 없음)
- **작업 브랜치**: `defense-formula-consideration-2026-08-25`(1개 커밋)
- **변경 파일**: `CLAUDE.md` 하나뿐(코드 변경 없음, 순수 설계 문서 — `src/character.js`/`skill-table.json` 등 실제 로직·데이터는 전혀 안 건드림)

## 배경 요약

지난 세션(`cave-monster-chain-concept-2026-08-24`, 이미 병합됨)에서
"방어력 공식 재검토 아이디어"(`100/(100+D)`)를 "고려 단계, 기록만"으로
남겨뒀었는데, 이번 세션에서 사용자가 **구체적인 문제 제기**를 함:
"방어관통 효과가 있다면 그걸 써서 얻는 이점이 있어야 하는데, RealDEF가
90 이상이 되면 방어관통의 효과가 점점 없어지는 게 아닌가?"

`src/character.js`의 `takeDamage()`를 다시 확인한 결과, 이건 실제
설계 결함으로 확인됨:

- 기존 방어관통 필드(`options.ignoreBonusDefPct`)는 설계상 2단계(bonus,
  버프성 방어력)만 무시하고, 1단계(`realDef` 퍼센트 경감)는 아예
  건드리지 않음(의도적 설계 — 주석에 명시).
- 그런데 1단계 공식 `max(0,1-realDef/100)`이 `realDef≥100`에서 완전
  포화(0% 통과)되면, 그 뒤 2단계는 항상 `max(0, 0-bonus)=0`이 되어서
  **방어관통 비율을 얼마나 줬든 이미 0인 값에서 더 뺄 게 없어 완전히
  무의미해짐**. 즉 RealDEF/MDEF≥100인 상대(예: `"???"`의 MDEF=200)에게는
  방어관통이 있는 스킬/장비가 문자 그대로 아무 효과가 없음.

논의를 거쳐 다음을 합의:
1. 1단계 공식을 `100/(100+D)`로 교체(완전 포화 없음, "DEF 1당 유효HP
   1%"와 수학적으로 동치임을 역산으로 확인).
2. 방어관통은 새 필드로 D값 자체를 깎는 방식(`effectiveD = realD -
   armorPierce`)으로 구현 — 기존 `ignoreBonusDefPct`와는 별개.
3. 2단계(bonus DEF)는 방어관통과 완전히 무관하게 유지 — "관통해도
   버프 방어력은 별개의 값어치를 가진다"는 사용자 근거를 raw 600 공격
   예시로 검증 완료(관통 100%여도 bonus 500이 붙어있으면 여전히 500을
   깎아냄).
4. `"???"`의 MDEF=200(주 워크스페이스의 클로드가 설계한 값, 이번
   세션 담당자와 다름)을 새 공식으로 재검증 — 감쇄율 67%로 여전히
   강력, "마법으로 공략하기 힘들다"는 원래 의도에 새 공식이 오히려
   더 정확히 부합한다고 결론(재설계 불필요).

**사용자 확정 지시**: "방어관통은 스킬에도 추가될 가능성이 있어서,
나중에 추가 설계(아마 속성별 추가 데미지 시스템과 함께)가 들어갈 때
확실히 하겠다. 현재로썬 md에 높은 순위의 고려사항으로 남겨달라."

→ **이번 세션에도 여전히 미적용**. 파일 맨 위(intro 바로 아래)에 새
`##` 섹션 "[고려 단계·우선순위 높음] 방어력 공식 개편 — 방어관통이
실질적 가치를 갖도록"을 추가하고, 기존 동굴 섹션의 "방어력 공식
재검토 아이디어" 문단 끝에 이 새 섹션을 가리키는 후속 갱신 문구를
붙임(내용 중복 없이 상호 참조만).

## 병합 전 체크리스트

`CLAUDE.md` 순수 텍스트 추가라 코드 실행 검증 대상 아님. `node --check`/
`demo-*.js` 불필요. 코드/데이터 파일(`src/character.js`,
`skill-table.json` 등)은 이번 브랜치에서 전혀 안 건드렸음 — 실제 공식
교체·방어관통 필드 구현은 **다음 설계 세션**(방어관통/속성데미지 스킬
설계와 함께)까지 보류.

## 병합 방법

```bash
git fetch origin
git show origin/defense-formula-consideration-2026-08-25:CLAUDE.md | head -80   # 새 섹션 확인
git merge origin/defense-formula-consideration-2026-08-25
git rm diff-defense-formula-consideration-2026-08-25.md
git commit
```

## 다음 세션에서 이어갈 것 (구현 미착수)

방어관통 필드의 정확한 이름·부여 방식(스킬 필드 vs `grantPassiveMod`
vs 장비)과 속성별 추가 데미지 시스템을 함께 설계할 때:
- 1단계 공식(`100/(100+D)`) 교체가 기존 확정 밸런스에 미치는 영향을
  `simulate.js`로 재검증(저DEF 개체는 영향 미미, 중간DEF 개체는 통과율이
  꽤 올라감).
- MDEF/DEF 대칭 적용 여부(현재 논의는 물리/마법 동일 공식 전제).
- 방어관통이 real만 깎는지, %기반 관통도 별도로 둘지.
