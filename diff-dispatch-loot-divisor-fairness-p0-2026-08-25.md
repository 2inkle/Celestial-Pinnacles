# diff-dispatch-loot-divisor-fairness-p0-2026-08-25.md — [P0] 파견 전리품 정산 구조 결함 (2026-08-25)

⚠ 새 명명 규칙 실천(브랜치 고유 파일명). 병합 검토 끝나면
`git rm diff-dispatch-loot-divisor-fairness-p0-2026-08-25.md`로 제거 권장.

- **기준 브랜치**: `main`(커밋 `678b90c`, 드리프트 없음)
- **작업 브랜치**: `dispatch-loot-divisor-fairness-p0-2026-08-25`(5개 커밋:
  `db756e1` 최초 기록, `7c76d74` 본 companion 문서, `b9103e6` "51%" 표현
  재정정, `7128e5e` companion 문서 갱신, `9317d54` 사용자 최종 검증
  내용 기록)
- **변경 파일**: `CLAUDE.md` 하나뿐(코드 변경 없음, 순수 설계 문서 —
  이번엔 실제 `web/dispatch.html` 수정은 하지 않음, 방향만 기록)

## 배경 요약 — ⚠ 우선순위 P0

사용자가 "고블린의 왕" 파견에서 "왕이 나오는 파견은 2000턴 내내 계속
나오고, 안 나오는 파견은 아예 안 나온다"는 이분법적 패턴을 신고함.

**조사 과정**(관리자 계정으로 라이브 사이트(`2inkle.github.io`)에
직접 접속해 브라우저 도구로 진행):
1. `spawnEnemies()`/`runBattle()` 정적 코드 리뷰 — 전부 매 호출 새로
   계산되는 구조, 코드만으론 이상 없음.
2. `spawnEnemies('goblin-king')` 830회 순수 호출 — 킹 등장 38.1%로
   정상 분산 확인.
3. 사용자가 실제 사용한 파티(로열가드/인퀴지터/화이트아크/카디널/
   둠로드, 전원 Lv30)를 장비까지 그대로 재현 — 킹전 100% 승리(3턴),
   2000턴당 약 830전, 킹 등장 40~50%.
4. 실제 파견 버튼을 라이브로 5~10회 직접 클릭해 비교 — 일부 회차는
   왕 전용 드랍이 "전혀 안 보임", 나머지는 "현지 39~59 → ×1"로 표시.
5. `spawnEnemies`/`runBattle`을 몽키패치해 실제 파견 버튼 클릭
   경로를 직접 계측 — "오래된 바퀴 자국"의 전투 중 원본 누적량이
   42였는데 최종 화면엔 아예 안 보임. `web/dispatch.html`의
   `finalLoot`(`floor(raw/100) + 확률적 반올림`) + `quantity>0`
   필터를 대조해서, raw=42 → 42% 확률로만 1개, 58%는 0개(화면에서
   행 자체가 사라짐)임을 확인.

**1차 결론(불완전, 사용자가 재반박)**: "0 아니면 40~60"은 실제로는
"최종 0개 또는 1개"의 동전 던지기이고 버그가 아니라고 판단했으나,
사용자가 **더 깊은 구조적 문제**를 짚음:

> "고블린의 왕 전투에서 왕의 실제 등장확률은 50% 내외인데, 그보다
> 낮은 등장률을 가지는 몬스터가 생기거나, 실제로 희귀해야 할
> 아이템이 파견 한 번에 나와버리는 사태가 생길 수 있다. 드랍된
> 전리품을 floor값으로 확인하기 때문에, 100턴짜리 전투 20회 중
> 단 1번이라도 등장해도 드랍된 것으로 쳐준다는 가정으로도 '희귀
> 아이템이 30분 단위로 하나씩 복사'되는 상황이 발생한다."

**재계산으로 확인된 진짜 문제**: `LOOT_DIVISOR=100`은 드랍 확률
자체를 100분의 1로 줄이는 게 아니라 **"파견 동안 쌓인 원본 누적
수량(킬 횟수×드랍확률)을 100으로 나누는" 것**이고, **파견 안에서
같은 몬스터를 몇 번 잡는지엔 어떤 상한도 없다.** 실측: 왕은 파견
1회에 약 330~350회 잡힘 → "왕의 대검"(15% 드랍) 원본 기대량
≈51개 → **파견 한 번에 51% 확률로 획득**. "직접 도전이 유리해야
한다"는 설계 의도가 완전히 무력화됨.

**재정정(3번째 커밋, `b9103e6`)**: "51% 확률로 획득"이라는 위 표현
자체도 문제를 축소한 것이라는 사용자의 재지적: "실제로 350회 등장해서
51개가 드랍이 되건 1개가 드랍이 되건, warehouse에 전달되는 수량은
1개가 아닌가 — 훨씬 치명적인 문제다." 확인 결과 정확함 — `finalLoot`은
`floor(raw/100)`이 정수부를 담당하는데 **raw<100인 한 이 정수부는
항상 0**이라 최종 수량은 무조건 "0 또는 1"로 상한이 걸린다. raw=51이든
raw=99든 결과의 형태가 똑같다 — 즉 진짜 문제는 "51%의 확률로 획득"이
아니라 **"파밍량과 무관하게 정산 결과는 항상 0 또는 1개뿐이고, 그 1개를
얻을 확률만 raw에 비례해 커진다"**는 것.

추가로 확인한 질문(raw=1이면 결과가 통째로 말소되는가?) — 아니다:
`0.01` 확률로 정확히 1개가 살아남는다(기댓값은 수학적으로 보존됨).
그런데 이 사실이 오히려 사용자가 짚은 **더 근본적인 역전 구조**를
드러낸다: 드랍확률 100%짜리 흔한 아이템(고블린의 이빨)은 raw가
5,900~6,100대라 `floor` 몫만으로 58~61개가 **확정 지급**되는 반면,
희귀 아이템은 raw가 항상 100 미만이라 `floor` 몫이 **영원히 0** —
오직 마지막 동전 던지기 하나에 전부가 걸린다. **"흔한 아이템 = 파밍량에
비례한 안정적 누적" vs "희귀 아이템 = 파밍량과 무관한 전부-아니면-전무
도박"** — 340번 잡으나 40번 잡으나(둘 다 raw<100이면) 결과의 성격이
똑같다는 것 자체가, 희귀 아이템의 "희귀도" 설계 의도를 완전히
무의미하게 만든다.

**최종 검증(5번째 커밋, `9317d54`)**: 사용자가 위 진단을 6개 항목으로
요약하고 확인을 요청 → 재확인 과정에서 조사관(나)이 두 군데를 과잉
정정했다가 사용자가 재지적해 되돌림, 그 과정에서 새로운 배경 사실도
하나 확인됨:
- **raw는 실제로 일어난 드랍의 합계다**(가상의 기댓값 아님) —
  `src/engine.js:545-548`의 `grantKillReward()`가 몬스터를 잡을 때마다
  `dropTable`의 각 항목에 개별적으로 `Math.random() > drop.chance`를
  굴려 성공한 것만 진짜 수량으로 누적시킴. 문제를 더 무겁게 만드는
  확인 — 마지막 변환이 뭉개는 건 "실제로 이미 일어난 획득"이다.
- **"최소 20회"는 정확함**(조사관이 과잉 정정했다가 되돌림) —
  `TURN_BUDGET=2000`÷전투당 `maxTurns=100`(`web/dispatch.html:335,339`)
  = 구조적 하한 20회. 830회는 특정 파티가 강해서 상한에 붙은 특수
  사례일 뿐, 사용자의 "20회"는 엔진 설정 자체에서 나오는 일반론.
- **"raw 199개도 1%의 확률로 raw 1개와 같은 결과를 낼 수 있다"도
  정확함**(조사관이 과잉 정정했다가 되돌림) — raw=1→(0개 99%/1개 1%),
  raw=199→(1개 1%/2개 99%) — 두 경우 모두 "1개"가 나올 수 있어
  공평성 문제 제기에 정확히 부합.
- **새로 발견한 배경**: 지금의 floor+확률적 반올림 방식 자체가 이미
  한 차례 고쳐진 결과물임(`web/dispatch.html:360-366` 주석) — 예전엔
  순수 내림이라 원본 누적량이 100 미만인 아이템은 파견으로 영구히
  0개(구조적으로 불가능)였고, 지금 방식은 "희귀할 뿐 불가능하지는
  않게" 하려던 의도적 개선. **다음 수정은 이 예전 버그로 되돌아가지
  않아야 한다는 제약이 걸림.**

전체 배경/근거는 `CLAUDE.md`의 새 섹션 "[P0] 파견 전리품 정산 —
'몬스터를 몇 번 잡았는지'에 상한이 없어 희귀템 희귀도가 통째로
무력화됨" 및 그 아래 "재정정"/"코드 재확인" 소단락에 그대로
있음(아래 diff 참고).

## 병합 전 체크리스트

`CLAUDE.md` 순수 텍스트 추가라 코드 실행 검증 대상 아님 — 이번 브랜치는
실제 `web/dispatch.html` 코드는 전혀 안 건드림(방향만 기록, 구현은
다음 세션).

## 병합 방법

```bash
git fetch origin
git show origin/dispatch-loot-divisor-fairness-p0-2026-08-25:CLAUDE.md | head -100   # 새 섹션 확인
git merge origin/dispatch-loot-divisor-fairness-p0-2026-08-25
git rm diff-dispatch-loot-divisor-fairness-p0-2026-08-25.md
git commit
```

## 전체 diff

```diff
diff --git a/CLAUDE.md b/CLAUDE.md
index 915e80a..748e0ce 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -6,6 +6,192 @@ JS로 만드는 턴제 전투 시뮬레이션 웹게임. 패턴 빌드로 스킬
 테마(마을→왕국→그 뒤) 하나만 구현돼 있고, 이걸로 엔진과 성장곡선이
 유효한지 검증하는 게 목표.
 
+## [P0] 파견 전리품 정산 — "몬스터를 몇 번 잡았는지"에 상한이 없어 희귀템 희귀도가 통째로 무력화됨 (2026-08-25)
+
+### 최초 증상(사용자 신고)
+"고블린의 왕" 파견을 여러 번 보내보면, **왕이 등장하는 파견은 2000턴
+내내 왕이 계속 나오고, 안 나오는 파견은 2000턴 내내 아예 안 나온다"**는
+이분법적 패턴이 관측됨 — 왕 전용 드랍(오래된 바퀴 자국/왕의 대검)이
+"전혀 안 나옴" 또는 "40~60개대"라는 두 결과만 존재한다고 보고.
+
+### 조사 과정
+1. **정적 코드 리뷰**: `web/battle-encounters.js`의 `spawnEnemies()`/
+   `weightedPick()`, `web/battle-adapter.js`의 `runBattle()` — 전부
+   매 호출마다 새로 계산되는 구조라 코드만 봐서는 이상 없음.
+2. **브라우저로 라이브 사이트 직접 접속**(관리자 계정, `2inkle.github.io`)
+   해서 `spawnEnemies('goblin-king')`을 830회 순수 호출 — 킹 등장
+   38.1%로 정상 분산 확인(고정 패턴 없음).
+3. **사용자가 실제로 사용한 정확한 파티**(로열가드/인퀴지터/화이트아크/
+   카디널/둠로드, 전원 Lv30)를 실제 장비까지 그대로 불러와 재현 —
+   이 파티는 킹전을 **100% 승리**(3턴), 2000턴에 약 830전, 킹 등장
+   약 40~50%.
+4. **실제 파견 버튼을 라이브로 5~10회 직접 클릭**해 결과 화면을
+   비교 — 처음 5회 중 2회는 왕 전용 드랍이 "전혀 안 보임", 나머지는
+   "현지 39~59 → ×1" 형태로 나타남. 이후 `spawnEnemies`/`runBattle`을
+   몽키패치해 실제 파견 버튼 클릭 경로 안에서 오가는 데이터를 직접
+   가로채 계측.
+5. **결정적 확인**: 계측 로그상 "오래된 바퀴 자국"의 전투 중 원본
+   누적량이 42였는데, 그 판의 최종 화면엔 그 항목이 아예 없었음.
+   `web/dispatch.html`의 `finalLoot` 계산(`quantity =
+   floor(raw/LOOT_DIVISOR) + (Math.random() < 소수부 ? 1 : 0)`,
+   `LOOT_DIVISOR=100`)과 `.filter((l) => l.quantity > 0)`를 대조—
+   raw=42면 `floor(0.42)=0`이고 42% 확률로만 1개, 58%는 0개(화면에서
+   행 자체가 사라짐). **1차 결론(불완전)**: "0 아니면 40~60"으로 보인
+   현상은 실제로는 "최종 0개 또는 1개"의 동전 던지기이고, 사용자가
+   본 "40~60"은 최종 개수가 아니라 성공 시 같이 뜨는 "현지 XX" 원본
+   수치였다 — 여기까지는 버그가 아니라고 판단했었음.
+
+### 진짜 문제(사용자가 짚어서 재수정)
+1차 결론은 "이 정산 방식 자체가 왜 구조적으로 위험한지"를 놓치고
+있었음. 사용자의 재반박: **"고블린의 왕 전투에서 왕의 실제 등장확률은
+50% 내외이고, 그보다 낮은 등장률을 가지는 몬스터가 생기거나, 실제로
+희귀해야 할 아이템이 파견 한 번에 나와버리는 사태가 생길 수 있다.
+드랍된 전리품을 floor값으로 확인하기 때문에, 100턴짜리 전투 20회
+중 단 1번이라도 등장해도 드랍된 것으로 쳐준다는 가정으로도 '희귀
+아이템이 30분 단위로 하나씩 복사'되는 상황이 발생한다."**
+
+이 지적이 정확함을 재계산으로 확인:
+- `LOOT_DIVISOR=100`은 "드랍 확률 자체를 100분의 1로 줄인다"가
+  아니라 **"이번 파견 동안 쌓인 원본 누적 수량(raw = 킬 횟수 ×
+  드랍확률)을 100으로 나눈다"**는 뜻이다.
+- **파견 안에서 같은 몬스터를 몇 번 잡는지는 어디에도 상한이 없다.**
+  실측: 왕은 파견 1회(2000턴)에 약 330~350회 등장·전승(이 파티
+  기준). "왕의 대검"(직접 도전에서 "만나기도 어렵고 나오기도 어려운
+  최상위 소재"로 설계된 15% 드랍)의 원본 기대량 = 340×0.15≈**51개**
+  → `51/100=0.51`, **파견 한 번에 51% 확률로 획득**. "직접 도전이
+  유리해야 한다"는 설계 의도(코드 주석: "LOOT 쪽 100은... 희귀
+  아이템은 직접 도전이 유리해야 한다는 역할 분담 의도만 보고 정한
+  값")가 완전히 무력화됨.
+- 근본 원인: `LOOT_DIVISOR=100`이라는 설계는 **"몬스터를 파견 중
+  100번 넘게 잡는 일은 없을 것"이라는 암묵적 전제** 위에 서 있는데,
+  실제로는 파견이 길고(2000턴) 승률까지 높은 조합에서는 그 전제가
+  손쉽게 깨짐 — 몬스터가 100번보다 훨씬 자주 잡히는 순간부터 나눗값의
+  억제력이 통째로 사라진다.
+
+### 재정정 — "51% 확률로 획득"은 그 자체로도 문제를 축소해서 기록한 것이었음
+위 계산을 처음 기록할 때 "원본 기대량 51 → 파견 한 번에 51% 확률로
+획득"이라고 적었는데, 사용자가 이 표현조차 문제를 과소평가한 것이라고
+재지적함: **"실제로 350회 등장해서 51개가 드랍이 되건 1개가 드랍이
+되건, 실제로 warehouse에 전달되는 아이템의 수량은 1개가 아닌가.
+훨씬 치명적인 문제다."**
+
+확인 결과 정확한 지적임 — `finalLoot`의 계산은
+`quantity = floor(raw/100) + (Math.random() < (raw/100 - floor(raw/100)) ? 1 : 0)`
+이고, **raw가 100 미만인 한(즉 "왕을 100번 넘게 잡지 않는 한")
+`floor(raw/100)`은 항상 0이라 최종 수량의 상한이 무조건 1개다.**
+raw=51이든 raw=99든 최종 결과는 "0개 또는 1개"로 동일 — **340번 잡아서
+쌓은 51개어치의 "파밍 성과"가 실제로는 단 한 개의 동전 던지기로
+소진**되고, 나머지 50개어치 가치는 어디에도 반영되지 않고 그냥
+버려진다. 즉 원래 표현해야 했던 문제는 "51%의 확률로 획득한다"가
+아니라 **"파밍량이 얼마든(raw가 1이든 99든) 정산 결과는 항상 0 또는 1
+개뿐이고, 그 1개조차 얻을 확률만 raw에 비례해 커진다"**는 것.
+
+**추가로 확인한 질문 — raw=1이면 그 결과가 아예 말소되는가?**
+아니다: `exact = 1/100 = 0.01`, `floor(0.01)=0`이지만 그 뒤
+`Math.random() < 0.01`이면 그대로 +1이 붙는다 — 즉 **정확히 1%의
+확률로 1개가 살아남는다(장기 기댓값은 수학적으로 정확히 보존됨)**.
+"raw가 낮으면 무조건 지워진다"는 아니고, "raw가 낮을수록 그 1개조차
+살아남을 확률이 raw에 비례해 작아질 뿐"이다.
+
+**그런데 이 사실이 오히려 사용자가 짚은 더 근본적인 역전 구조를
+드러낸다.** 사용자의 재반박: **"희귀 아이템의 등장 확률이 '존재는
+한다'를 정면에서 반박하면서, 동시에 비교적 덜 희귀한 아이템이
+확정적으로 드랍되도록 되어있는 구조가 아닌가."** — 실측으로 확인:
+드랍확률 100%짜리 흔한 아이템("고블린의 이빨")은 830전투 동안
+raw가 5,900~6,100대까지 쌓이고, `floor(raw/100)` 자체가 이미
+58~61개를 **소수부 반올림과 무관하게 확정 지급**한다. 반면 희귀
+아이템(왕의 대검 등)은 아무리 킬 카운트를 쌓아도 raw가 항상 100
+미만이라 **`floor` 몫이 영원히 0** — 오직 마지막 동전 던지기 하나에
+전부가 걸린다. 결과적으로 이 시스템은:
+- **흔한 아이템**: 파밍량에 정직하게 비례해 안정적으로(변동성 낮게)
+  누적 지급됨 — "흔하다"는 설계 의도가 수치로 정확히 구현됨.
+- **희귀 아이템**: 파밍량과 사실상 무관하게(raw가 두 배가 되어도
+  "0 또는 1"이라는 결과의 형태 자체는 안 바뀜) 매 파견 단 한 번의
+  전부-아니면-전무 도박으로 환원됨 — 이게 "희귀 아이템이 아예 안 나올
+  수도 있다"는 존재 자체를 부정하는 게 아니라(1% 확률로도 나오긴
+  하므로), **"많이 파밍했다는 사실이 만들어내야 할 신뢰도/확실성의
+  차이"를 통째로 지워버린다는 점에서 문제**. 340번 잡으나 40번
+  잡으나(어느 쪽이든 raw<100이면) 결과의 성격이 똑같은 동전 던지기라는
+  것 자체가, "직접 도전보다 파견이 오히려 근거 없이 관대해지거나
+  근거 없이 인색해질 수 있다"는 양방향 리스크를 만든다.
+
+이 역전 구조는 "정직하고 공평한 회귀방법"(아래 해결 방향 참고)이
+반드시 고쳐야 할 핵심 — 흔한 아이템처럼 **파밍량에 비례한 점진적
+확률/수량 곡선**을 희귀 아이템에도 동일하게 적용해야, "많이 죽여도
+결과는 똑같이 도박"이라는 이 왜곡이 사라진다.
+
+### `Heart of Deepstone`(동굴 보스 고유 유니크)에 그대로 적용되는 위험
+동굴 보스가 (설계 목표대로 상향되더라도) 언젠가 파티가 안정적으로
+이기는 상대가 되면, 파견 한 번에 수백 번 잡히는 건 시간문제다.
+Heart of Deepstone의 드랍 확률을 아무리 낮게 잡아도(예: 2~3%),
+원본 기대량이 100을 넘기는 순간부터 파견만 반복해도 사실상 확정
+획득이 된다 — "직접 도전으로만 의미 있게 얻는 보스 고유 유니크"라는
+카드 시스템 설계 취지(`cave-card-theme-and-boss-drop-2026-08-25`
+브랜치) 자체가 무너짐.
+
+### 코드 재확인 — 위 진단이 근거하는 실제 메커니즘, 세부 검증 완료
+사용자가 요약한 이해를 6개 항목으로 재확인한 결과, 두 가지 세부
+정밀화와 하나의 새로운 배경 사실이 추가로 확인됨(사용자가 검토 후
+"문제없다"고 최종 확인한 부분까지 포함):
+
+1. **`raw`는 가상의 기댓값이 아니라 실제로 일어난 드랍의 합계다.**
+   `src/engine.js:545-548`의 `grantKillReward()`를 보면, 몬스터를
+   잡을 때마다 `dropTable`의 각 항목에 대해 **`Math.random() >
+   drop.chance`를 개별적으로 굴려** 성공 여부를 판정하고, 성공하면
+   실제 `quantity`를 더한다. 즉 `raw`(=`loot.quantity` 누적값)는
+   "킬 횟수×드랍확률"이라는 계산값이 아니라, **파견 내내 실제로
+   벌어진 개별 전투들에서 진짜로 성립한 드랍 판정의 합**이다. 이
+   사실은 문제를 더 무겁게 만든다 — 마지막의 `floor(raw/100)+확률적
+   반올림` 변환이 뭉개는 건 가상의 숫자가 아니라 "이미 실제로
+   일어난 획득"이기 때문.
+2. **"최소 20회"는 특정 사례(고블린의 왕)의 실측치가 아니라, 엔진
+   설정 자체에서 나오는 구조적 하한이며 정확하다.** `TURN_BUDGET=
+   2000`, 전투당 `maxTurns=100`(`web/dispatch.html:335,339`)이므로
+   최악의 경우(매 전투가 상한 100턴을 전부 소모)에도 파견 1회당
+   최소 `2000/100=20`회의 전투가 보장된다. 830회는 이 하한이 아니라
+   테스트에 쓰인 파티가 강해서(전투당 턴 소모가 짧아서) 상한 쪽에
+   붙은 특수 사례였을 뿐 — 조사관(나)이 이 특정 사례의 실측치와
+   사용자가 말한 일반론적 하한을 혼동해 "20회는 과소평가"라고 잘못
+   재반박했었고, 사용자가 이를 정정함.
+3. **"raw 1개와 raw 199개가 1%의 확률로 같은 결과(1개)를 낼 수
+   있다"는 것도 정확한 문제 제기다.** raw=1 → `exact=0.01` → 결과는
+   0개(99%) 또는 1개(1%). raw=199 → `exact=1.99` → 결과는 1개(1%)
+   또는 2개(99%). 두 경우 모두 "1개"라는 결과가 나올 수 있고, 199배
+   더 파밍해도 운이 나쁘면 1배 파밍과 최종 결과가 구분되지 않을 수
+   있다는 뜻 — 공평성 문제 제기에 정확히 부합. "199는 floor로 이미
+   1개가 확정되니 예시가 부적절하다"는 조사관(나)의 재반박은 "가능한
+   최댓값이 다르다"는 사실과 "그럼에도 최솟값 쪽에서 두 경우가 겹칠
+   수 있다"는 사용자의 실제 논지를 혼동한 것이었고, 사용자가 이를
+   정정함.
+4. **새로 발견한 배경 — 지금의 floor+확률적 반올림 방식은 이미 한
+   차례 고쳐진 결과물이다.** `web/dispatch.html:360-366` 주석: "예전엔
+   그냥 내림이라, 파견 한 번의 원본 누적량이 100을 못 넘는 아이템
+   (왕관 조각·섭정의 인장·바퀴 자국 등)은 영구히 0개가 됐음 —
+   확률이 낮은 게 아니라 구조적으로 불가능했음." 즉 순수 내림 방식은
+   지금보다 더 나쁜 상태(희귀 아이템이 파견으로는 절대 안 나옴)였고,
+   지금 방식은 "희귀할 뿐 불가능하지는 않게" 만들려는 의도적 개선이었다.
+   **이 배경은 다음 수정 방향에 실질적 제약을 건다** — 이번에 새로
+   설계할 정산 방식은 "raw<100 구간에서 파밍량에 비례한 신뢰도를
+   회복"하면서도, 이 예전 버그(순수 내림으로 회귀 → 희귀 아이템 영구
+   0개)를 다시 만들지 않아야 한다.
+
+### 다음 세션에서 이어갈 것 — 정직하고 공평한 회귀(정산) 방법을 고려해야 함
+**해결 방향(제안, 미구현)**: 원본 수량(quantity)을 나중에 한 번에
+나누는 지금 방식 대신, **"몬스터를 죽인 횟수(킬 카운트) 자체를 먼저
+`LOOT_DIVISOR`로 압축(확률적 반올림)한 뒤, 그 압축된 횟수만큼만 실제
+드랍 확률로 독립적으로 다시 굴리는"** 방식으로 바꾸는 게 방향성 있어
+보임 — 예: 킬 카운트 340 → 압축 3~4회 → 그 3~4회에 대해서만 각각
+`Math.random() < dropChance`를 다시 판정. 이러면 기댓값(장기적으로
+같은 아이템을 얻는 평균 빈도)은 동일하게 유지되면서(선형성 보존),
+**몬스터가 파견에서 아무리 자주 잡혀도 그 아이템은 "직접 도전과 똑같은
+확률로, 다만 시도 횟수만 압축된" 정직한 희귀도를 유지**하게 됨(자주
+마주치는 몬스터라고 해서 희귀템의 억제력이 사라지지 않음). 이번
+세션엔 방향만 확정 — 실제 구현·`simulate.js` 검증·기존 파견 경제
+전반(경험치/골드 나눗값 문제 등 기존에 이미 알려진 갭 포함)과의
+정합성 검토는 다음 세션(주 워크스페이스)으로 넘김. **동굴 전용
+사안이 아니라 파견 시스템 전체의 구조적 문제** — Heart of Deepstone
+설계를 확정하기 전에 반드시 먼저 해결해야 할 선행 과제로 기록.
+
 ## Sheet 스킬 카드 효과 표시 — "27종 전수 등록"(2026-08-22) 이후에도 남아있던 누락 2건 수정 (2026-08-24)
 
 바로 아래(2026-08-22) "실전투 신고 4건 일괄 수정"의 4번 항목("Sheet 화면
```

## 다음 세션에서 이어갈 것 (구현 미착수)

주 워크스페이스에서: "몬스터를 죽인 횟수 자체를 먼저 압축한 뒤, 압축된
횟수만큼만 독립적으로 드랍을 재판정"하는 방식으로 `web/dispatch.html`의
`finalLoot`/전리품 누적 로직을 재설계·구현하고 `simulate.js`로 검증.
기존에 이미 알려진 파견 경제 갭(경험치/골드가 같은 `EXP_DIVISOR`를
공유하는 문제 등)과의 정합성도 함께 검토. Heart of Deepstone 등 향후
보스 고유 유니크 드랍 설계를 확정하기 전에 반드시 먼저 해결할 것.
