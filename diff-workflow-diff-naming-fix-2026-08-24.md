# diff-workflow-diff-naming-fix-2026-08-24.md — companion 문서 고정 파일명 문제 해결 (2026-08-24)

⚠ **이 파일 자체가 새 규칙의 첫 실천 사례입니다** — 고정 `diff.md`
대신 브랜치명이 들어간 `diff-workflow-diff-naming-fix-2026-08-24.md`로
커밋합니다. 병합 검토가 끝나면 main에 남길 필요 없는 임시 문서이므로,
병합 시 `git rm diff-workflow-diff-naming-fix-2026-08-24.md`로 빼고
커밋하는 걸 권장합니다.

- **기준 브랜치**: `main`(커밋 `89eeb0c`, 드리프트 없음)
- **작업 브랜치**: `workflow-diff-naming-fix-2026-08-24`
- **변경 파일**: `CLAUDE.md` 하나뿐(엔진/UI 코드 변경 없음, 순수 워크플로
  문서 추가)

## 배경 요약

주 워크스페이스에게 "diff.md를 fetch해서 main에 병합할 수 있는지"
확인해달라는 요청을 받고 실제로 읽기전용 dry-run 병합을 해봤더니, 두
기능 브랜치(`skill-card-effect-fix-2026-08-24`,
`fullassist-heal-mastery-2026-08-24`)를 **개별로** 병합하는 건 문제없지만
**연달아 둘 다** 병합하면 두 곳에서 충돌:
- `diff.md` — 둘 다 저장소 루트에 같은 파일명으로 서로 다른 내용을
  추가해서 **add/add 충돌**.
- `CLAUDE.md` — 둘 다 파일 맨 위 같은 위치에 각자 새 섹션을 추가해서
  위치 충돌(내용 자체는 안 겹쳐서 두 섹션 다 유지하면 되는 사소한 문제).

이번 커밋은 이 문제를 향후 재발하지 않게 `CLAUDE.md`와 Claude 메모리
양쪽에 기록한 것 — companion 문서(`diff.md`류)는 브랜치마다 고유한
파일명(`diff-<브랜치명>.md`)을 쓰고, 병합 시 제거하는 걸 표준으로 삼음.

## 병합 전 체크리스트

이 세션 환경에 Node.js가 없어서 확인 못 함 — 다만 이번 변경은
`CLAUDE.md`(순수 텍스트 문서) 하나뿐이라 코드 실행 검증 자체가 불필요함.
`node --check`/`demo-*.js` 대상 아님.

**병합 시 주의**: 이 브랜치를 다른 두 브랜치(`skill-card-effect-fix-
2026-08-24`, `fullassist-heal-mastery-2026-08-24`)와 함께 순서대로
병합할 계획이라면, `CLAUDE.md` 위치 충돌이 3개 브랜치 조합에서도 날 수
있음 — 각 병합마다 두 섹션(또는 세 섹션)을 전부 유지하는 방향으로 수동
해결하면 됨(삭제할 내용 없음, 순서/위치만 조정).

## 병합 방법

```bash
git fetch origin
git show origin/workflow-diff-naming-fix-2026-08-24:CLAUDE.md | head -50   # 새 섹션 확인
git merge origin/workflow-diff-naming-fix-2026-08-24
git rm diff-workflow-diff-naming-fix-2026-08-24.md   # 병합 검토용 임시 문서라 main엔 불필요
git commit
```

## 전체 diff

```diff
diff --git a/CLAUDE.md b/CLAUDE.md
index 4366944..228eccf 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -6,6 +6,36 @@ JS로 만드는 턴제 전투 시뮬레이션 웹게임. 패턴 빌드로 스킬
 테마(마을→왕국→그 뒤) 하나만 구현돼 있고, 이걸로 엔진과 성장곡선이
 유효한지 검증하는 게 목표.
 
+## 보조 워크스페이스 협업 시 주의 — companion 문서(diff.md류) 고정
+파일명 금지 (2026-08-24)
+
+이 저장소는 실제 작업이 여러 워크스페이스(주 워크스페이스 + git/Node가
+제한된 보조 워크스페이스)에서 나뉘어 진행될 수 있음. 보조 워크스페이스가
+자체적으로 `Celestial-Pinnacles-review` 같은 별도 clone을 만들어 기능
+브랜치를 origin에 push하고, 그 브랜치에 배경/전체 diff/병합 체크리스트를
+담은 companion 문서(예: `diff.md`)를 같이 커밋해서 주 워크스페이스가
+전수조사 없이 리뷰·병합할 수 있게 하는 방식을 쓰고 있음.
+
+**실제로 겪은 문제**: 두 기능 브랜치(`skill-card-effect-fix-2026-08-24`,
+`fullassist-heal-mastery-2026-08-24`)가 각각 저장소 루트에 고정
+파일명 `diff.md`를 커밋했음. 각 브랜치를 *개별로* main에 병합하는 건
+문제없었지만, 읽기전용 dry-run으로 두 브랜치를 연달아 병합해보니
+`diff.md`에서 **add/add 충돌**이 남(서로 다른 브랜치가 같은 경로에 다른
+내용의 새 파일을 추가). 동시에 `CLAUDE.md`도 두 브랜치가 이 파일 맨 위
+같은 삽입 위치에 각자 새 섹션을 추가해서 위치 충돌이 났음(이쪽은 내용
+자체는 안 겹쳐서 두 섹션 다 유지하면 되는 사소한 문제).
+
+**원칙**: companion 리뷰 문서는 **브랜치마다 고유한 파일명**을 쓴다(예:
+`diff-<브랜치명>.md`). 그리고 이 문서는 병합 검토가 끝나면 main에 영구히
+남길 이유가 없는 임시 문서이므로, 병합 시 `git rm diff-*.md`로 빼고
+커밋하는 걸 표준으로 삼는다 — 그러면 애초에 이름이 우연히 안 겹쳐도
+결국 main에는 하나도 안 쌓인다. 여러 기능 브랜치가 아직 병합 전 상태로
+동시에 origin에 떠 있을 수 있는 이 워크플로 특성상, "여러 브랜치가 같은
+경로에 같은 파일명으로 뭔가를 추가하면 병합 시 충돌한다"는 게 일반
+원칙임(`skill-table.json`/`LEGACY_*_SEED`처럼 "데이터가 두 군데 있다"는
+기존 문제와는 결이 다르지만, "여러 소스가 같은 자리를 두고 경합한다"는
+근본 성격은 같음).
+
 ## 개조된 장비가 상점 구매/전리품과 잘못 합쳐지던 버그 — 3곳에 동일 패턴 (2026-08-22)
```
